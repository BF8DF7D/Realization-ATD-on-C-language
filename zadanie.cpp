#include "Talon.h"
#include "Disease.h"
#include <locale.h>
#include <iostream>
#include <conio.h>
#include <Windows.h>

int main()
{
    SetConsoleCP(1251);
    SetConsoleOutputCP(1251);

    GetsDiseaseData();
/*    Doctor name;
    name = GetsDoctorData();
    PutsDoctorInfo(name);
    
    puts("");
    
    Talon num;
    num = GetsTalonData(&name);
    PutsTalonInfo(num);
  */  
}